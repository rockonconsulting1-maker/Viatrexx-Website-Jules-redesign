# Experience Memory

- Project is a static mirror created with HTTrack.
- Assets are stored in domain-named directories in the root.
- Many relative links in `index.html` incorrectly use `../` when they should point to the root.
- Navigation and UX components are often duplicated across pages or embedded in custom code blocks.
