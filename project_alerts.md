# Project Alerts

- Link/navigation changes that might affect SEO:
  - Fixed systemic broken asset paths in `index.html` and `home.html` (previously pointing to non-existent `../` directories).
  - Improved link integrity by ensuring all internal navigation points to valid local files.
  - Added `rel="noopener noreferrer"` to external links.
