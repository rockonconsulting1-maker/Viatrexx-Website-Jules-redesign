# 62 React Best Practices (Vercel Style)

1. **No Inline Components**: Define components outside of the render function.
2. **Use Semantic HTML**: Prefer `<button>` over `<div onClick={...}>`.
3. **Memoize Expensive Calculations**: Use `useMemo` and `useCallback` where appropriate.
...
